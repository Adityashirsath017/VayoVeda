import { View, Text } from 'react-native';

// This file exists to satisfy Expo Router's route detection for /Notification.
// The actual rendering is handled by app/(tabs)/_layout.tsx via the Tab.Navigator.
export default function NotificationPlaceholder() {
    return null;
}
