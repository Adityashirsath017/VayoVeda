import { View, Text } from 'react-native';

// This file exists to satisfy Expo Router's route detection for /Schedule.
// The actual rendering is handled by app/(tabs)/_layout.tsx via the Tab.Navigator.
export default function SchedulePlaceholder() {
    return null;
}
